import {hot} from 'react-hot-loader/root';
import React from 'react';

const App = props => {
    return (
        <div className="App">
            Hello World
        </div>
    );
};

export default hot(App);
